server {
    listen 80;

    location /api {
        proxy_pass http://backend:9000;
        # sesuaikan jika backend pakai php-fpm langsung, biasanya butuh fastcgi_pass, bukan proxy_pass
    }

    location / {
        proxy_pass http://frontend:80;
    }
}